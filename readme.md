<center>
  <h3>herd</h3>
  <img src="assets/herd.png"/>
  <sub>manage dev servers through raycast</sub>
</center>

#### setup

1. Clone the repo
2. Type "Import Extension"
3. Select 'herd' repo
4. Run `yarn build` or `npm build`
5. Type `herd` into raycast and select the `Manage Servers` extension
6. Import your servers
